# 候选池上限调整为 40 个

## 变更

- 候选池最大容量由 50 个调整为 40 个。
- 容量计算继续包含 `watching` 和 `bought` 两类候选。
- 池满时继续优先保留所有 `bought` 候选，只对 `watching` 候选执行优先级裁剪或替换。
- 服务启动和定时对账时，超过 40 个的历史候选会按现有优先级规则裁剪到 40 个。

## 验证

- 覆盖候选池上限固定为 40。
- 覆盖满池拒绝、优先项目替换、超限裁剪和已买入候选保留逻辑。
