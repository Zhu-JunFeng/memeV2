# Telegram 交易通知

## 通知范围

- 买入信号和卖出信号写入数据库后发送通知。
- 买入交易和卖出交易成功落库后发送通知。
- 模拟盘和实盘都发送，并在消息中明确标记交易模式。
- 页面在模拟盘和实盘之间成功切换后发送通知，包含原模式、新模式、当前交易钱包和切换时间；模式未变化时不重复发送。
- 重复 Redis 信号、报价拒绝、执行失败的订单不发送“交易成功”通知。

## 配置

配置位于 `telegram` 节点，包括 `enabled`、`bot_token` 和 `chat_id`。生产环境的真实 Token 与私聊 ID 仅写服务器配置，不进入代码仓库。
